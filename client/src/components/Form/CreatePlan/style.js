import { makeStyles } from "@mui/styles";


export default makeStyles((theme) => {
    return {
      root: {
        display: "flex",
        height: "100%",
        marginLeft: "20px",
        marginRight: "20px",
        flexDirection: "Column",
      },
    };
  });
